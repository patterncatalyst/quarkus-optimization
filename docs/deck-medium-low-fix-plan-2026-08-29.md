# MEDIUM/LOW accuracy fix plan — 2026-08-29

Branch `fix/deck-medium-low`. Fixes the remaining MEDIUM+LOW findings from
`deck-accuracy-review-2026-08-28.md` (issue #2). Surgical `<a:t>` run swaps keyed
by (file, exact string); README block replacements; footer Option A. All facts
verified vs current docs + eval logs. Display position ≠ file number after the two
injected slides (slide55=pos4, slide56=pos19) — edits are keyed by FILE.

## Slide-face edits (exact OLD → NEW)

**slide18.xml** — LGTM acronym (MEDIUM, verified)
- `Grafana LGTM: Loki (logs) + Grafana (viz) + Tempo (traces) + Prometheus (metrics)` → `Grafana LGTM: Loki (logs) + Grafana (viz) + Tempo (traces) + Mimir (metrics, Prometheus-compatible)`

**slide48.xml** — Vector API JEP (MEDIUM, JEP 508 verified)
- `FFM API: JEP 454 (finalized JDK 22)  ·  Vector API: JEP 460 (incubating)  ·  jextract: separate download at jdk.java.net` → `FFM API: JEP 454 (finalized JDK 22)  ·  Vector API: JEP 508 (Tenth Incubator, JDK 25)  ·  jextract: separate download at jdk.java.net`

**slide49.xml** — C++21 + jextract (MEDIUM+LOW, verified)
- `C++21 features (spans, ranges) inside` → `C++20 features (spans, ranges) inside`
- `// Generated by jextract from jvmstats.h` → `// Bound by hand via Linker.downcallHandle (jextract can also generate this)`
- `  JvmStats.recommend_gc$MH();` → `  JvmStats.recommend_gc$handle();` (keep 2 leading spaces)

**slide51.xml** — int size (LOW, verified)
- `8 bytes × N, dense, cache-friendly` → `4 bytes × N, dense, cache-friendly`

**slide54.xml** — MaxMetaspaceSize (MEDIUM, judgment)
- `→  Add -XX:MaxMetaspaceSize=256m to cap Metaspace growth on framework-heavy apps` → `→  Add -XX:MaxMetaspaceSize sized from observed peak + headroom (e.g. 512m for framework-heavy apps)`

**slide34.xml** — JSON size (LOW, verified)
- `JSON text  (~400 bytes)` → `JSON text  (~220 bytes)` (2 spaces before `(`)

**slide9.xml** — -Xmx8192m (LOW, ×3 identical runs — change all)
- `-Xmx8192m (default)` → `-Xmx8192m (no limit → 25% of 32 GB node)`

**slide10.xml** — ParallelGCThreads + throttle (MEDIUM+LOW)
- `JVM defaults ParallelGCThreads to host CPU count. On a 64-core node with 4 CPU limit, GC spawns 64 threads competing for 4 CPUs. Fix: -XX:ParallelGCThreads=4` → `Without a CPU limit the JVM sizes GC threads to the node — capped above 8 cores (8 + (N-8)×5/8, so 43 on a 64-core node), ignoring your request. Set a CPU limit (the JVM reads the cgroup quota) or pin -XX:ParallelGCThreads=4`
- `GC is CPU-intensive. CPU limits throttle GC threads mid-pause. A 100ms G1GC pause becomes 400ms under throttle. Set CPU limit ≥ 2× request.` → `GC is CPU-intensive. CPU limits throttle GC threads mid-pause — e.g. a 100ms G1GC pause can stretch to ~400ms (illustrative). Set CPU limit ≥ 2× request.`

**slide8.xml** — ActiveProcessorCount (MEDIUM)
- `-XX:ActiveProcessorCount=N for fractional CPUs — prevents JVM assuming 0 GC threads` → `-XX:ActiveProcessorCount=N for fractional/limited CPUs — overrides an inflated processor count (the JVM never drops below 1 GC thread)`

**slide42.xml** — ZGC p99 (MEDIUM, verified via demo-06.log = GC-pause delta, no p99)
- `ZGC vs G1GC p99 latency under identical GC pressure — measured live` → `ZGC vs G1GC GC-pause behaviour under identical pressure — ZGC targets sub-ms pauses by design`
- `#  ZGC  p99:  &lt;1ms` → `#  ZGC  pauses: sub-ms (design target)`
- `#  G1GC p99: 50-200ms` → `#  G1GC pauses: 50-200ms`

**slide31.xml** — PetClinic + JEP 516 (MEDIUM+LOW, verified)
- `~40% faster startup — Spring PetClinic 2s→1s` → `~42% faster startup — PetClinic 4.5s→2.6s (JEP 483)`
- `Previously G1GC only — now works with ZGC` → `Previously the AOT cache was ZGC-incompatible — now GC-agnostic (works with all GCs)`

**slide16.xml** — CDS default (LOW, JEP 341 verified)
- `CDS (Java 25 — enabled by default)` → `CDS (default since JDK 12 — still on)`
- `Java 25: active by default — no flags needed for JDK class sharing` → `Default CDS archive since JDK 12 (JEP 341) — no flags needed for JDK class sharing`

**slide3.xml** — cold start (LOW, judgment)
- `4–8s` (stat box, en-dash) → `2–5s`
- `4-8s JVM init delays scale-out during traffic bursts — SLAs breached` → `2-5s JVM init delays scale-out during traffic bursts — SLAs breached`

**slide44.xml** — headroom headline (LOW)
- `Right-Sizing Analysis — p99 Observed + 30% Headroom` → `Right-Sizing Analysis — p99 Observed + Headroom (CPU 30% / Mem 25%)`

**slide43.xml** — source/scale (LOW, verified sample data)
- `Source: 14 days of Prometheus data · prod-ocp-cluster-01` → `Source: 14 days of bundled sample Prometheus metrics · demo dataset`
- `typical 20-service cluster` → `extrapolated to a 20-service cluster`

**slide28.xml** — cold-start consistency (LOW, recommended)
- `Quarkus JVM baseline: ~0.3-0.8s (vs Spring Boot ~4-8s — already 10x faster!)` → `Quarkus JVM baseline: ~0.3-0.8s (vs Spring Boot ~2.4-4.2s — already ~5-10x faster)`

**slide13.xml** — ParallelGCThreads consistency (recommended)
- `Must match your CPU limit — defaults to host CPU count` → `Match your CPU allocation — reads the cgroup quota when a limit is set`

**slide53.xml** — anti-pattern consistency (recommended)
- `64-core node + 2-CPU container = 64 GC threads competing for 2 CPUs` → `64-core node, no CPU limit = GC threads sized to the node (43+), not your request`

## Notes edits
**notesSlide14.xml** — `Spring PetClinic dropped from 2 seconds to 1 second. That's the same 40 percent you see in all the benchmarks.` → `Spring PetClinic dropped about 42 percent — roughly 4.5 seconds to 2.6 seconds — in the JEP 483 benchmark.` (preserve surrounding quote/run)
**notesSlide31.xml** — `Spring PetClinic: 2s → 1s — 40% improvement.` → `Spring PetClinic: ~4.5s → ~2.6s — ~42% improvement.` (2 leading spaces)
**notesSlide47.xml** — `when CPU p99 is 4× the median` → `when CPU p99 is 3× the median` (analyze.py L75 = 3×; 2 leading spaces)
**notesSlide37.xml** — `   both ends — gRPC gives you 3-4x throughput, 38% less CPU, and streaming` → `   both ends — gRPC gives you 3-4x throughput in production and streaming` (3 leading spaces)
**notesSlide43.xml** — `Outcome numbers come from real Prometheus data on a 7-service cluster.` → `Outcome numbers come from bundled sample Prometheus data modelling a 7-service cluster.`
**notesSlide6.xml** — `Heap is only ONE of six regions.` → `Heap is only ONE of five regions.`
**notesSlide3.xml** — `The 60% figure is from Red Hat's production customer data. Most Java teams don't realise they're overprovisioning by 2-4x.` → `The 60% figure is an illustrative industry estimate of memory overprovisioning. Most Java teams don't realise they're overprovisioning by 2-3x.`
**notesSlide10.xml** — `ParallelGCThreads: on a 64-core node with a 2-CPU container limit, JVM defaults to 64 GC threads competing for 2 CPUs. Set this = your CPU limit. Always.` → `ParallelGCThreads: with a CPU limit set, the JVM reads the cgroup quota and sizes GC threads to it. The trap is requests-but-no-limit — then it sizes to the node, capped above 8 cores (8 + (N-8)×5/8, e.g. 43 on 64 cores). Set a CPU limit or pin -XX:ParallelGCThreads to your allocation.`

Verify-only (no edit): notesSlide20 Micrometer extension already consistent.

## Footer — Option A (drop running total)
On the 36 slide files carrying it, change the footer run:
`Taming the JVM: Java Optimization on OpenShift   |   N/26` → `Taming the JVM: Java Optimization on OpenShift`
(delete the `   |   N/26` suffix; the ‹#› auto-field stays where present). Files: 2,3,4,6-29,31,33,36,53,54,55,56 (per-file N varies — match each full run).

## README.md
Replace "The Slides" section (≈L93-142), the Repository Structure pptx lines (≈L21-27), and Quick Navigation slides row (≈L50) to describe the ONE real 56-slide deck (see planner output for exact new blocks). Remove references to the 6 non-existent decks.

## Deferred
ONNX slide bottom card nudge — not clipped; off-slide footer is deck-wide convention. Do in PowerPoint if wanted.

## Acceptance
Post-edit greps return 0: `JEP 460`, `C++21`, `Tempo (traces) + Prometheus`, `MaxMetaspaceSize=256m`, `-Xmx8192m (default)`, `recommend_gc$MH`, `8 bytes × N`, `4× the median`, `N/26`; README has none of the 6 fake deck filenames. PetClinic reads ~42%/4.5s→2.6s. Deck opens clean, 56 slides, xmllint OK.
